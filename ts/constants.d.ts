import { LoadingState } from './types';
export declare const LOAD_POLICIES: {
    CACHE_FIRST: string;
    CACHE_AND_LOAD: string;
    LOAD_ONLY: string;
};
export declare const STATES: {
    [key: string]: LoadingState;
};
